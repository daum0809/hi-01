# 학습 노트 — 1. AI는 잘 만드는데, 내 말은 왜 안 통할까

> 커리큘럼: 바이브 코딩으로 웹 만들기_Agt2 > 2.[프로젝트] 웹페이지를 이루는 세 겹 & 게임만들기

## 오늘의 역할

이끄미 :
시간지킴이 :

## 오늘의 주제

웹페이지를 이루는 세 겹 — 구조(HTML)·표현(CSS)·동작(JavaScript)

## 오늘의 목표

* 웹페이지가 구조·표현·동작이라는 성격이 다른 세 겹으로 이루어져 있음을 이해한다.
* 각 겹이 맡는 일과, AI에게 그 겹을 고쳐달라고 할 때 쓰는 말을 구분한다.
* AI가 만든 화면에 문제가 생겼을 때, 어느 겹을 봐야 할지 판단하는 눈을 기른다.

## 오늘의 학습 내용

**1. 웹페이지의 세 겹**
* 웹페이지는 하나의 덩어리처럼 보이지만, 성격이 다른 세 가지가 겹쳐 있다.
  * 구조 — 화면에 무엇이 있는지
  * 표현 — 그것이 어떻게 보이는지
  * 동작 — 사용자가 무언가 했을 때 무엇이 바뀌는지
* 개발자는 이 셋을 각각 HTML·CSS·JavaScript로 나누어 다룬다.

**2. HTML — 무엇이 담기는지 적는 문서**
* 제목, 문단, 버튼 같은 정보에 태그로 의미를 붙인다.
```html
<!-- h1 = 가장 큰 제목, button = 누르는 버튼 -->
<h1>내 첫 웹페이지</h1>
<button>시작하기</button>
```

**3. CSS — 어떻게 보일지 정하는 규칙**
* 색, 간격, 크기, 배치를 정한다.
```css
/* "h1 제목은 이렇게 보여라" — 파란색으로, 가운데 정렬로 */
h1 {
  color: blue;
  text-align: center;
}
```

**4. JavaScript — 무엇이 바뀔지 정하는 프로그래밍 언어**
* 사용자가 뭔가 했을 때(클릭 등) DOM의 클래스·속성·상태를 바꿔 반응을 만든다.
```js
// 버튼을 누르면(click) 인사말 창을 띄웁니다
button.addEventListener('click', () => {
  alert('안녕하세요!');
});
```

**5. 세 이름이 따로 있는 이유**
* HTML·CSS·JS는 한 사람이 같은 날 설계한 세트가 아니라, 웹이 자라면서 서로 다른 문제를 풀기 위해 서로 다른 시기에 태어난 도구들이다(자세한 내력은 다음 강의 "세 겹을 나누어 이해하기 — 이름에 담긴 사연"에서 다룸).

**6. 예측 문제 — 어느 겹을 고쳐야 할까**
| 증상 | 고칠 겹 | 이유 |
| --- | --- | --- |
| 안내 문구가 업무에서 쓰는 말과 맞지 않는다 | HTML | 화면에 담기는 내용(텍스트)의 문제이므로 구조 쪽 |
| 카드가 모바일에서 좁아져 글자가 겹친다 | CSS | 간격·크기·배치, 즉 보이는 방식의 문제 |
| 버튼을 눌러도 아무 반응이 없다 | JavaScript | 클릭했을 때 무엇이 바뀌어야 하는데 그 동작이 없거나 잘못됨 |

**7. 오늘의 핵심 한 문장**
> 웹페이지는 구조(HTML)·표현(CSS)·동작(JS) 세 겹이고, 셋을 나눠 AI에게 지시하면 원하는 화면을 빠르게 만들고 고칠 수 있다.

**8. 겹별 요약표**
| 겹 | 담당하는 일 | AI에게 시킬 때의 말 | 결과 판단 질문 |
| --- | --- | --- | --- |
| HTML | 화면에 무엇이 있는지 정함 | "제목, 안내 카드, 버튼을 의미에 맞게 배치해줘" | 필요한 정보와 업무 용어가 빠지지 않았는가 |
| CSS | 그것이 어떻게 보이는지 정함 | "모바일에서도 읽히게 간격, 색, 글자 크기를 조정해줘" | 눈이 어디로 가는지, 깨지는 구간은 없는가 |
| JavaScript | DOM의 클래스·속성·상태를 바꿔 동작을 정함 | "버튼을 누르면 화면의 내용이 바뀌게 해줘" | 클릭·입력·상태 변화가 기대대로 일어나는가 |

## 오늘의 질문사항

* "안내 문구가 업무 용어와 맞지 않는다"를 HTML로 분류했는데, 문구(텍스트 콘텐츠) 자체를 구조 문제로 봐도 되는지, 아니면 콘텐츠와 구조를 별도로 나눠 생각해야 하는지 좀 더 명확히 하고 싶다.
* HTML·CSS·JS가 각각 다른 시기에 태어난 "이름에 담긴 사연"이 궁금하다 — 다음 강의에서 이어서 확인할 예정.

## 오늘의 회고

* 새롭게 알게 된 것: 웹페이지 문제를 "구조/표현/동작" 세 겹으로 나눠 보면, AI에게 "이상해요" 대신 "이건 CSS 문제야" 식으로 구체적으로 짚을 수 있다는 것.
* 도움이 되었던 방법: 각 겹의 실제 코드 예시(`h1`/`button`, `color`/`text-align`, `addEventListener`)를 문법을 외우지 않고 눈으로만 훑은 것 — "이렇게 생겼구나" 정도의 감을 잡는 데 충분했다.
* 잘 안된 점 / 보완 계획: 예측 문제(세 증상 → 어느 겹?) 답을 감으로만 냈고 실제로 검증하지 못했다. 다음 실습(4강 "AI로 게임 만들기")에서 AI가 만든 화면에 비슷한 증상을 재현해 직접 확인해볼 예정.
