# 학습 노트 — 3.[오늘의 CS] 동기·비동기, 프로세스·스레드·코어

> 커리큘럼: 바이브 코딩으로 웹 만들기_Agt2 > 2.[프로젝트] 웹페이지를 이루는 세 겹 & 게임만들기

## 오늘의 역할

이끄미 :
시간지킴이 :

## 오늘의 주제

동기·비동기, 프로세스·스레드·코어 — 웹이 멈추지 않는 이유

## 오늘의 목표

* 동기/비동기 개념을 언어(코드) 차원, 프로세스/스레드/코어를 운영체제·하드웨어 차원으로 구분해서 이해한다.
* JavaScript가 싱글 스레드이기 때문에 비동기 처리가 필요한 이유를 설명할 수 있다.
* "버벅인다" 같은 막연한 표현 대신, 동기/비동기·스레드 개념을 써서 AI에게 구체적으로 요청하는 법을 익힌다.

## 오늘의 학습 내용

**1. 동기 vs 비동기 — 전화와 문자**
* 동기(synchronous): 전화처럼 상대가 응답할 때까지 기다리고, 그동안 다른 일을 못 한다.
* 비동기(asynchronous): 문자처럼 보내놓고 하던 일을 계속하다가, 응답이 오면 그때 처리한다.
* 카페 진동벨 비유: 커피 나오는 시간(작업 시간)은 동일하지만, 기다리는 동안 다른 일을 할 수 있느냐가 다르다.

**2. JavaScript는 싱글 스레드**
* 브라우저 안의 JS는 한 번에 한 가지 일만 처리한다(일꾼 한 명).
* 동기로만 처리하면, 서버 응답을 기다리는 몇 초 동안 화면 전체(스크롤, 클릭, 재생 등)가 멈춘다(blocking).
* 그래서 `fetch`, `setTimeout` 같은 오래 걸리는 작업은 비동기로 걸어두고 즉시 다음 코드로 넘어간다.
* 완료된 작업 결과는 대기열에 쌓이고, 지금 일을 마친 JS 일꾼이 이 대기열을 돌며 처리한다 — 이 구조가 **이벤트 루프(event loop)**.
* `async`, `await`, `.then()`, `setTimeout`을 보면 "여기는 기다리지 않고 걸어두는 부분"이라고 읽으면 된다.
* 참고: [MDN 이벤트 루프 문서](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop)

**3. 프로세스 vs 스레드 — 작업실과 일꾼**
* 프로세스(process): 실행 중인 프로그램 하나, 독립된 메모리(작업실)를 가짐. 다른 프로세스의 메모리를 침범하지 못함.
* 스레드(thread): 프로세스(작업실) 안에서 일하는 일꾼. 같은 프로세스의 스레드끼리는 메모리를 공유함.
* 2008년 크롬은 "탭마다 독립된 프로세스"를 선택 — 웹이 문서에서 애플리케이션으로 바뀌며 탭 하나의 오류가 브라우저 전체를 죽이는 문제를 막기 위함. ([크롬 멀티프로세스 아키텍처 설계 문서](https://www.chromium.org/developers/design-documents/multi-process-architecture/))

| | 멀티스레딩 | 멀티프로세싱 |
| --- | --- | --- |
| 구조 | 한 작업실에 일꾼 여러 명 | 작업실 자체를 여러 개 |
| 메모리 | 공유 | 독립 |
| 강점 | 가볍고 데이터 교환이 빠름 | 하나가 죽어도 나머지는 안전 |
| 약점 | 동시에 같은 데이터를 건드리면 꼬일 수 있음(레이스 컨디션) | 무겁고 작업실 간 데이터 전달 비용이 있음 |
| 예 | 브라우저 한 탭 안의 렌더링·네트워크 일꾼들 | 크롬의 탭들 |

**4. 코어 — 물리적 일손**
* 코어(core): CPU 안에서 실제로 계산하는 물리적 단위. 코어 8개면 진짜 동시에 8가지 일을 할 수 있음.
* 2000년대 클럭 속도 경쟁이 발열/전력 한계에 부딪히면서 "하나를 더 빠르게" 대신 "적당히 빠른 것을 여러 개"로 방향 전환 → 멀티코어 시대.
* Herb Sutter, "The Free Lunch Is Over"(2005): 이제 프로그램은 가만히 있으면 빨라지지 않고, 여러 일손을 쓰도록 설계해야 빨라진다.
* 시분할(time-sharing): 코어 수보다 훨씬 많은 프로세스/스레드를 OS가 아주 빠르게 번갈아 실행해 동시처럼 보이게 함. 1960년대 MIT CTSS에서 시작, 컴퓨터 비밀번호도 이때 생긴 부산물.

**5. 전체 그림**
코어(물리적 일손) → OS가 프로세스/스레드에 자원 배분 → 브라우저는 탭마다 프로세스, 그 안에서 여러 스레드로 작업 → JS 일꾼은 한 명이라 오래 걸리는 일은 비동기로 걸어둠. 구글 지도가 손동작을 따라오면서 동시에 데이터를 받고, 유튜브가 영상 재생 중에 댓글을 계속 불러오는 것이 이 구조 위에서 일어나는 일이다.

**6. 판단 문제**
> A. 버튼을 눌렀을 때 점수를 1 올려 화면에 표시 — 로컬에서 즉시 끝나는 가벼운 연산이라 **동기**로 처리해도 된다.
> B. 버튼을 눌렀을 때 서버에서 랭킹 목록을 받아와 표시 — 네트워크 요청은 응답 시간이 불확실하고, 동기로 처리하면 그동안 싱글 스레드인 JS가 화면 전체를 멈춘다. 그래서 **비동기**로 처리해야 한다.

**7. 실습**
* macOS: `sysctl -n hw.ncpu` / Windows PowerShell: `echo $env:NUMBER_OF_PROCESSORS` 로 내 컴퓨터의 코어 수 확인.
* 확인한 코어 수와 활동 상태 보기/작업 관리자의 프로세스 수를 비교 — 일손보다 일감이 훨씬 많은데도 화면이 멈추지 않는 이유가 시분할.

## 오늘의 질문사항

* 싱글 스레드인 JS의 이벤트 루프와, 브라우저 자체의 멀티프로세스·멀티스레드 구조가 실제로 어떻게 맞물려 동작하는지 더 명확히 하고 싶다(탭 프로세스 안에 JS 스레드 외에 렌더링/네트워크 스레드가 별도로 있다는 것과의 관계).
* 레이스 컨디션(같은 메모리를 여러 스레드가 동시에 건드려 꼬이는 문제)이 실제 코드에서 어떤 상황으로 나타나는지 구체적인 예시가 더 필요하다.

## 오늘의 회고

* 새롭게 알게 된 것: 동기/비동기는 언어(코드) 차원의 개념이고, 프로세스·스레드·코어는 운영체제·하드웨어 차원의 개념이라는 계층 구분.
* 더 자세히 알게 된 것: 크롬이 탭마다 프로세스를 분리하게 된 배경 — AJAX 이후 웹이 문서에서 애플리케이션이 되면서 격리(안정성)가 중요해졌다는 맥락.
* 도움이 되었던 방법: 전화/문자, 카페 진동벨, 작업실/일꾼 비유가 개념을 오래 기억하는 데 도움이 됨. MDN 이벤트 루프 문서와 크롬 멀티프로세스 설계 문서를 함께 참고.
* 잘 안된 점 / 보완 계획: 개념은 이해했지만 아직 직접 코드(fetch, setTimeout, async/await)로 동기/비동기를 짜보지 않아 손에 익지 않았다. 다음 강의("AI로 게임 만들기")에서 실제로 비동기 코드를 써보면서 체득할 예정.
